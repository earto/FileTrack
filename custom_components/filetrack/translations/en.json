{
  "config": {
    "step": {
      "user": {
        "title": "Set up FileTrack",
        "description": "FileTrack will be activated immediately. Use the configuration menu to add folder sensors."
      }
    },
    "abort": {
      "already_configured": "FileTrack is already installed."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Manage Sensors",
        "menu_options": {
          "add_sensor": "Add a new folder sensor",
          "remove_sensor": "Remove existing sensors"
        }
      },
      "add_sensor": {
        "title": "Add new sensor",
        "data": {
          "name": "Sensor Name",
          "folder": "Folder Path",
          "filter": "Filter (e.g. *.jpg)",
          "sort": "Sort Criteria",
          "recursive": "Search Subfolders"
        },
        "errors": {
            "invalid_path": "Path must start with /config/www/"
        }
      },
      "remove_sensor": {
        "title": "Remove sensors",
        "description": "Select the sensor(s) you wish to remove.",
        "data": {
          "sensors": "Sensors to remove"
        }
      },
      "confirm": {
        "title": "Confirm removal",
        "description": "You are about to permanently remove the following sensor(s):\n\n**{names}**\n\nThis cannot be undone.",
        "data": {
          "confirm": "Yes, remove these sensor(s)"
        }
      }
    },
    "abort": {
      "no_sensors": "There are no sensors to remove.",
      "cancelled": "Operation cancelled."
    }
  }
}
